from unicodedata import name
from django.urls import path
from .views import *

urlpatterns = [
    
    # path('district',DistrictView.as_view(),name='district'),
    # path('district/<int:pk>',DistrictView.as_view(),name='district'),
    # path('block',BlockView.as_view(),name='block'),
    # path('panchayat',PanchayatView.as_view(),name='panchayat'),
    # path('village',VillageView.as_view(),name='village'),
    # path('division',DivisionView.as_view(),name='division'),
    # path('subdivision',SubDivisionView.as_view(),name='subdivision'),
    # path('section',SectionView.as_view(),name='section'),
    # path('tension-type',TensionTypeViewSet.as_view(),name='tension_type'),
    # path('tension-type/<int:pk>/',TensionTypeView.as_view(),name='tension_type'),








   
]